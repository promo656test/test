name: Update Permissions

on:
  push:
    branches:
      - main
    paths:
      - 'src/members-permissions.ts'

jobs:
  update-permissions:
    runs-on: ubuntu-latest

    steps:

      - name: Use environment variable env
        run: echo "TOKEN is ${{ env.API_GITHUB_TOKEN }}"

      - name: Use environment variable var
        run: echo "TOKEN is ${{ var.API_GITHUB_TOKEN }}"

      - name: Use runtime variable
        run: echo "TOKEN is $API_GITHUB_TOKEN"

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build 
        run: yarn build
        
      - name: Run update permissions script
        env:
          API_GITHUB_TOKEN: ${{ env.API_GITHUB_TOKEN }}
        run: yarn update-permissions
